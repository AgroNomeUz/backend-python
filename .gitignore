.env
db.sqlite3
/staticfiles
# User uploads (listing photos). Local disk is the interim store — §0b.
/media
api_contract.md

# Python bytecode — never useful in review, and it churns on every run
__pycache__/
*.py[cod]

# Local tooling
.idea/workspace.xml
.venv/
